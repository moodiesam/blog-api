extends layout

block content
  h1= title

  if posts_list.length 
    ul 
      each post in posts_list 
        li 
          a(href=post.url) #{post.title}
          p= post.timestamp
        hr
    
